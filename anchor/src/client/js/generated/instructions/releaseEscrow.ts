/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { ESCROW_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RELEASE_ESCROW_DISCRIMINATOR = new Uint8Array([
  146, 253, 129, 233, 20, 145, 181, 206,
]);

export function getReleaseEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RELEASE_ESCROW_DISCRIMINATOR
  );
}

export type ReleaseEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountFeeRecipient extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountMerchant extends string
        ? WritableAccount<TAccountMerchant>
        : TAccountMerchant,
      TAccountFeeRecipient extends string
        ? WritableAccount<TAccountFeeRecipient>
        : TAccountFeeRecipient,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      ...TRemainingAccounts,
    ]
  >;

export type ReleaseEscrowInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReleaseEscrowInstructionDataArgs = {};

export function getReleaseEscrowInstructionDataEncoder(): FixedSizeEncoder<ReleaseEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RELEASE_ESCROW_DISCRIMINATOR })
  );
}

export function getReleaseEscrowInstructionDataDecoder(): FixedSizeDecoder<ReleaseEscrowInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReleaseEscrowInstructionDataCodec(): FixedSizeCodec<
  ReleaseEscrowInstructionDataArgs,
  ReleaseEscrowInstructionData
> {
  return combineCodec(
    getReleaseEscrowInstructionDataEncoder(),
    getReleaseEscrowInstructionDataDecoder()
  );
}

export type ReleaseEscrowAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountMerchant extends string = string,
  TAccountFeeRecipient extends string = string,
  TAccountEscrow extends string = string,
> = {
  /** Merchant or authority may release */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  config?: Address<TAccountConfig>;
  merchant: Address<TAccountMerchant>;
  feeRecipient: Address<TAccountFeeRecipient>;
  escrow: Address<TAccountEscrow>;
};

export async function getReleaseEscrowInstructionAsync<
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountMerchant extends string,
  TAccountFeeRecipient extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: ReleaseEscrowAsyncInput<
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountMerchant,
    TAccountFeeRecipient,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ReleaseEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountMerchant,
    TAccountFeeRecipient,
    TAccountEscrow
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    merchant: { value: input.merchant ?? null, isWritable: true },
    feeRecipient: { value: input.feeRecipient ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.feeRecipient),
      getAccountMeta(accounts.escrow),
    ],
    data: getReleaseEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as ReleaseEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountMerchant,
    TAccountFeeRecipient,
    TAccountEscrow
  >);
}

export type ReleaseEscrowInput<
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountMerchant extends string = string,
  TAccountFeeRecipient extends string = string,
  TAccountEscrow extends string = string,
> = {
  /** Merchant or authority may release */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  config: Address<TAccountConfig>;
  merchant: Address<TAccountMerchant>;
  feeRecipient: Address<TAccountFeeRecipient>;
  escrow: Address<TAccountEscrow>;
};

export function getReleaseEscrowInstruction<
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountMerchant extends string,
  TAccountFeeRecipient extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: ReleaseEscrowInput<
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountMerchant,
    TAccountFeeRecipient,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): ReleaseEscrowInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSystemProgram,
  TAccountConfig,
  TAccountMerchant,
  TAccountFeeRecipient,
  TAccountEscrow
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    merchant: { value: input.merchant ?? null, isWritable: true },
    feeRecipient: { value: input.feeRecipient ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.feeRecipient),
      getAccountMeta(accounts.escrow),
    ],
    data: getReleaseEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as ReleaseEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountMerchant,
    TAccountFeeRecipient,
    TAccountEscrow
  >);
}

export type ParsedReleaseEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Merchant or authority may release */
    authority: TAccountMetas[0];
    systemProgram: TAccountMetas[1];
    config: TAccountMetas[2];
    merchant: TAccountMetas[3];
    feeRecipient: TAccountMetas[4];
    escrow: TAccountMetas[5];
  };
  data: ReleaseEscrowInstructionData;
};

export function parseReleaseEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedReleaseEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
      config: getNextAccount(),
      merchant: getNextAccount(),
      feeRecipient: getNextAccount(),
      escrow: getNextAccount(),
    },
    data: getReleaseEscrowInstructionDataDecoder().decode(instruction.data),
  };
}
