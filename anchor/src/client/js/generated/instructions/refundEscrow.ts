/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { ESCROW_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REFUND_ESCROW_DISCRIMINATOR = new Uint8Array([
  107, 186, 89, 99, 26, 194, 23, 204,
]);

export function getRefundEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_ESCROW_DISCRIMINATOR
  );
}

export type RefundEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBuyerAccount extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBuyerAccount extends string
        ? WritableAccount<TAccountBuyerAccount>
        : TAccountBuyerAccount,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      ...TRemainingAccounts,
    ]
  >;

export type RefundEscrowInstructionData = { discriminator: ReadonlyUint8Array };

export type RefundEscrowInstructionDataArgs = {};

export function getRefundEscrowInstructionDataEncoder(): FixedSizeEncoder<RefundEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_ESCROW_DISCRIMINATOR })
  );
}

export function getRefundEscrowInstructionDataDecoder(): FixedSizeDecoder<RefundEscrowInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundEscrowInstructionDataCodec(): FixedSizeCodec<
  RefundEscrowInstructionDataArgs,
  RefundEscrowInstructionData
> {
  return combineCodec(
    getRefundEscrowInstructionDataEncoder(),
    getRefundEscrowInstructionDataDecoder()
  );
}

export type RefundEscrowAsyncInput<
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountBuyerAccount extends string = string,
  TAccountEscrow extends string = string,
> = {
  /** Buyer or authority may refund */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  config?: Address<TAccountConfig>;
  buyerAccount: Address<TAccountBuyerAccount>;
  escrow: Address<TAccountEscrow>;
};

export async function getRefundEscrowInstructionAsync<
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountBuyerAccount extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: RefundEscrowAsyncInput<
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RefundEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    buyerAccount: { value: input.buyerAccount ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.buyerAccount),
      getAccountMeta(accounts.escrow),
    ],
    data: getRefundEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >);
}

export type RefundEscrowInput<
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountBuyerAccount extends string = string,
  TAccountEscrow extends string = string,
> = {
  /** Buyer or authority may refund */
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  config: Address<TAccountConfig>;
  buyerAccount: Address<TAccountBuyerAccount>;
  escrow: Address<TAccountEscrow>;
};

export function getRefundEscrowInstruction<
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountBuyerAccount extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: RefundEscrowInput<
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): RefundEscrowInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSystemProgram,
  TAccountConfig,
  TAccountBuyerAccount,
  TAccountEscrow
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    buyerAccount: { value: input.buyerAccount ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.buyerAccount),
      getAccountMeta(accounts.escrow),
    ],
    data: getRefundEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as RefundEscrowInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >);
}

export type ParsedRefundEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Buyer or authority may refund */
    authority: TAccountMetas[0];
    systemProgram: TAccountMetas[1];
    config: TAccountMetas[2];
    buyerAccount: TAccountMetas[3];
    escrow: TAccountMetas[4];
  };
  data: RefundEscrowInstructionData;
};

export function parseRefundEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRefundEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
      config: getNextAccount(),
      buyerAccount: getNextAccount(),
      escrow: getNextAccount(),
    },
    data: getRefundEscrowInstructionDataDecoder().decode(instruction.data),
  };
}
