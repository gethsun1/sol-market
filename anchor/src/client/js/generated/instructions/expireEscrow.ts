/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from 'gill';
import { ESCROW_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const EXPIRE_ESCROW_DISCRIMINATOR = new Uint8Array([
  49, 150, 54, 201, 45, 106, 39, 175,
]);

export function getExpireEscrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_ESCROW_DISCRIMINATOR
  );
}

export type ExpireEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountBuyerAccount extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountBuyerAccount extends string
        ? WritableAccount<TAccountBuyerAccount>
        : TAccountBuyerAccount,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireEscrowInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireEscrowInstructionDataArgs = {};

export function getExpireEscrowInstructionDataEncoder(): FixedSizeEncoder<ExpireEscrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_ESCROW_DISCRIMINATOR })
  );
}

export function getExpireEscrowInstructionDataDecoder(): FixedSizeDecoder<ExpireEscrowInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireEscrowInstructionDataCodec(): FixedSizeCodec<
  ExpireEscrowInstructionDataArgs,
  ExpireEscrowInstructionData
> {
  return combineCodec(
    getExpireEscrowInstructionDataEncoder(),
    getExpireEscrowInstructionDataDecoder()
  );
}

export type ExpireEscrowAsyncInput<
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountBuyerAccount extends string = string,
  TAccountEscrow extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  config?: Address<TAccountConfig>;
  buyerAccount: Address<TAccountBuyerAccount>;
  escrow: Address<TAccountEscrow>;
};

export async function getExpireEscrowInstructionAsync<
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountBuyerAccount extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: ExpireEscrowAsyncInput<
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  ExpireEscrowInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    buyerAccount: { value: input.buyerAccount ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }
  if (!accounts.config.value) {
    accounts.config.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([99, 111, 110, 102, 105, 103])),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.buyerAccount),
      getAccountMeta(accounts.escrow),
    ],
    data: getExpireEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireEscrowInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >);
}

export type ExpireEscrowInput<
  TAccountSystemProgram extends string = string,
  TAccountConfig extends string = string,
  TAccountBuyerAccount extends string = string,
  TAccountEscrow extends string = string,
> = {
  systemProgram?: Address<TAccountSystemProgram>;
  config: Address<TAccountConfig>;
  buyerAccount: Address<TAccountBuyerAccount>;
  escrow: Address<TAccountEscrow>;
};

export function getExpireEscrowInstruction<
  TAccountSystemProgram extends string,
  TAccountConfig extends string,
  TAccountBuyerAccount extends string,
  TAccountEscrow extends string,
  TProgramAddress extends Address = typeof ESCROW_PROGRAM_ADDRESS,
>(
  input: ExpireEscrowInput<
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >,
  config?: { programAddress?: TProgramAddress }
): ExpireEscrowInstruction<
  TProgramAddress,
  TAccountSystemProgram,
  TAccountConfig,
  TAccountBuyerAccount,
  TAccountEscrow
> {
  // Program address.
  const programAddress = config?.programAddress ?? ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    buyerAccount: { value: input.buyerAccount ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.buyerAccount),
      getAccountMeta(accounts.escrow),
    ],
    data: getExpireEscrowInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireEscrowInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountConfig,
    TAccountBuyerAccount,
    TAccountEscrow
  >);
}

export type ParsedExpireEscrowInstruction<
  TProgram extends string = typeof ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    systemProgram: TAccountMetas[0];
    config: TAccountMetas[1];
    buyerAccount: TAccountMetas[2];
    escrow: TAccountMetas[3];
  };
  data: ExpireEscrowInstructionData;
};

export function parseExpireEscrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedExpireEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      systemProgram: getNextAccount(),
      config: getNextAccount(),
      buyerAccount: getNextAccount(),
      escrow: getNextAccount(),
    },
    data: getExpireEscrowInstructionDataDecoder().decode(instruction.data),
  };
}
